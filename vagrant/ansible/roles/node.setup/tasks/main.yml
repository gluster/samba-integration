- name: Enable EPEL repository
  yum:
    name: epel-release
    state: latest

- name: Install basic tools
  yum:
   name: "{{ tools }}"
   state: latest
  vars:
    tools:
      - lvm2
      - firewalld
      - libselinux-python
