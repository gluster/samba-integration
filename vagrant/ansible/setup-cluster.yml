- hosts: cluster
  become: yes
  become_method: sudo
  vars_files:
    - cluster-vars.yml
  roles:
    - node.prep
    - node.test
    - node.setup
    - gluster.prep
    - gluster.infra
    - gluster.cluster
    - ctdb.setup
    - samba-glusterfs.setup
